<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <title>Gallery</title>
    <link rel="stylesheet" href="css/gallery.css" type="text/css" </head>
</head>

<body>
    <form method="POST" action="/registration">
        {{>header}}
        <div class="registerAccount">
            <h3>Account Registration</h3>
            <div class="registration-flex">
                <input class="register-item" type="email" name="acuserName" id="AcuserName" required placeholder="Email" value="">
                <input class="register-item" type="password" name="acpassword" id="Acpassword" required placeholder="Password"
                    pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
                    title="Must contain at least one  number and one uppercase and lowercase letter, and at least 8 or more characters"
                    value="">
                <input class="register-item" type="password" name="confirmPassword" id="confirmpassword" required placeholder="Confirm Password"
                    value="">
                <button class="btn btn-primary" id="register" type="submit" value="submit">Register</button>
            </div>
            <label id="messageCon" name="message">{{data}}</label>
        </div>

    </form>

</body>

</html>